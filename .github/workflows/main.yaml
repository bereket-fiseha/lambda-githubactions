name: LamdaDeploy


on:
 push:
   branches:
      - main

jobs:
   deploy_Lamda:
     runs-on: ubuntu-latest
     steps:
        - uses: actions/checkout@v2
        - uses: actions/setup-python-@v2 
          with:
              python-version: 3.8
